import * as math from "./math";
import "./style.css";

const multiply = (a) => a*8;

console.log("ES6 modules");

console.log("sum = " + math.sum(2, 3));
console.log("mult = " + multiply(5));
console.log("math.mult = " + math.multiply(5));